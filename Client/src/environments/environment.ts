export const environment = {
  production: false,
  /** Relative to dev-server origin when using proxy; full URL in production if needed */
  apiUrl: '/api',
  /** Static files from Express (product images); proxied in dev */
  assetBaseUrl: '',
};
